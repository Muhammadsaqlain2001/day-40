<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit your Chats</title>
</head>
<body>
    <p>Edit your Chats <strong> <%= chat._id %> </strong></p>
    <p>The chat was sent from <strong><%= chat.from %></strong>  to <strong><%= chat.to %></strong></p>
    <form method="post" action="/chats/<%= chat._id %>?_method=Put>">
        <textarea name="msg" rows="6" cols="30"><%= chat.msg %></textarea>
        <br><br>
        <button>update</button>
    </form>
</body>
</html>